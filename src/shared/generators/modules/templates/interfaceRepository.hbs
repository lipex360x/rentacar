import {{pascalCase moduleName}} from '@modules/{{camelCase moduleName}}/infra/typeorm/entities/{{pascalCase moduleName}}'

export interface CreateProps {
  data: string
}

export default interface I{{pascalCase moduleName}}Repository {
  create(data: CreateProps): Promise<{{pascalCase moduleName}}>
}
