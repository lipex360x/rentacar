import {{pascalCase entityName}} from '@modules/{{camelCase moduleName}}/infra/typeorm/entities/{{pascalCase entityName}}'
import I{{pascalCase moduleName}}Repository, { CreateProps } from '@modules/{{camelCase moduleName}}/repositories/interfaces/I{{pascalCase moduleName}}Repository'

export default class Fake{{pascalCase moduleName}}Repository implements I{{pascalCase moduleName}}Repository {
  private repository: {{pascalCase entityName}}[] = []

  async create ({ data }:CreateProps): Promise<{{pascalCase entityName}}> {
    const {{camelCase entityName}} = new {{pascalCase entityName}}()

    Object.assign({{camelCase entityName}}, {
      data,
      {...},
      created_at: new Date(),
      updated_at: new Date()
    })

    this.repository.push({{camelCase entityName}})

    return {{camelCase entityName}}
  }
}
