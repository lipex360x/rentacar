import { v4 as uuid } from 'uuid'

import {{pascalCase moduleName}} from '@modules/{{camelCase moduleName}}/infra/typeorm/entities/{{pascalCase moduleName}}'
import I{{pascalCase moduleName}}Repository, { CreateProps } from '../interfaces/I{{pascalCase moduleName}}Repository'

export default class Fake{{pascalCase moduleName}}Repository implements I{{pascalCase moduleName}}Repository {
  private repository: {{pascalCase moduleName}}[] = []

  async create ({ data }:CreateProps): Promise<{{pascalCase moduleName}}> {
    const {{camelCase moduleName}} = new {{pascalCase moduleName}}()

    Object.assign({{camelCase moduleName}}, {
      data,
      {...},
      created_at: new Date(),
      updated_at: new Date()
    })

    this.repository.push({{camelCase moduleName}})

    return {{camelCase moduleName}}
  }
}
