import { Router } from 'express'

import {{pascalCase name}}CreateController from '@modules/{{camelCase name}}/useCases/{{pascalCase name}}/Create/{{pascalCase name}}Create.controller'

const router = Router()

const {{name}}CreateController = new {{pascalCase name}}CreateController()

router.post('/', {{name}}CreateController.handle)

export default router
