import { Repository, getRepository } from 'typeorm'

import {{pascalCase moduleName}} from '@modules/{{camelCase moduleName}}/infra/typeorm/entities/{{pascalCase moduleName}}'
import I{{pascalCase moduleName}}Repository, { CreateProps } from '@modules/{{camelCase moduleName}}/repositories/interfaces/I{{pascalCase moduleName}}Repository'

export default class {{pascalCase moduleName}}Repository implements I{{pascalCase moduleName}}Repository {
  private repository: Repository<{{pascalCase moduleName}}>

  constructor () {
    this.repository = getRepository({{pascalCase moduleName}})
  }

  async create ({ data }: CreateProps): Promise<{{pascalCase moduleName}}> {
    //  TO DO
  }
}
